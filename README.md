### Setup dependencies
    yarn install

### Install gulp globally(if neccessary)
    npm install gulp -D
    npm install gulp-cli -g

### Development
    gulp serve

### Make build
    gulp build

### zip build
    gulp zip
